# TODO

- Add a production contact form endpoint and set `VITE_CONTACT_ENDPOINT` in deployment environment variables.
- Confirm the final production domain, then update the canonical URL in `index.html` if it differs from `https://lunerya.dev/`.
